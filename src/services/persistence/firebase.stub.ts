/**
 * Stub para futura implementação Firebase (Firestore / Realtime DB).
 *
 * Passos típicos:
 * 1. firebase init + regras de segurança
 * 2. Implementar PersistenceAdapter com getDoc/setDoc ou onSnapshot
 * 3. Em main.tsx ou AppDataProvider, trocar localStorageAdapter por firebaseAdapter
 *
 * Exemplo de interface (já definida em ./types.ts):
 *   getSongs, saveSongs, getSetlists, saveSetlists
 */

export {}
